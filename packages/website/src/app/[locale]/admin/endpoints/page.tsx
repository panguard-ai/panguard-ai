import EndpointsContent from './EndpointsContent';

export const metadata = {
  title: 'Endpoints - Admin - Panguard AI',
  description: 'Manage and monitor connected endpoints',
  robots: 'noindex',
};

export default function EndpointsPage() {
  return <EndpointsContent />;
}
