[build]
buildCommand = "cat package.json | head -3 && sed -i 's/\"workspace:\\*\"/\"*\"/g' package.json && rm -rf node_modules dist tsconfig.tsbuildinfo && npm install && npx tsc --build --force && grep -c 'scan-events' dist/server.js && echo 'BUILD OK: new endpoints verified'"
