# -------------------------------------------------------
# Threat Cloud Environment Variables
# 威脅雲環境變數
#
# Copy to .env and fill in your values:
#   cp deploy/.env.example .env
# -------------------------------------------------------

# =========================
# REQUIRED (必填)
# =========================

# API keys for authentication (comma-separated, min 32 chars each)
# Generate: openssl rand -hex 32
TC_API_KEYS=

# =========================
# RECOMMENDED (建議設定)
# =========================

# Domain for HTTPS (Caddy auto-obtains Let's Encrypt cert)
TC_DOMAIN=tc.panguard.ai

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=https://panguard.ai,https://www.panguard.ai

# =========================
# OPTIONAL (選配)
# =========================

# Backup schedule (cron expression, default: daily 3am UTC)
# TC_BACKUP_CRON=0 3 * * *

# Max backup retention (days, default: 30)
# TC_BACKUP_RETENTION_DAYS=30

# Seed feeds to import on first run (comma-separated or "all")
# Available: ipsum, feodo, blocklist, spamhaus, urlhaus, threatfox
SEED_FEEDS=all
