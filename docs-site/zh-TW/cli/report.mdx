---
title: "panguard report"
description: "產生符合主要安全框架的合規與稽核報告。"
icon: "file-chart-column"
---

`report` 指令從您的掃描和 Guard 資料產生專業的合規報告。報告可對應 ISO 27001、SOC 2、NIST CSF 和 Taiwan TCSA 等框架，讓稽核準備變得簡單直接。

## 使用方式

```bash
panguard report <subcommand> [options]
```

## 子指令

| 子指令 | 說明 |
| --- | --- |
| `generate` | 產生合規報告 |
| `summary` | 顯示簡要的合規摘要，不產生完整報告 |
| `list-frameworks` | 列出所有支援的合規框架 |
| `validate` | 驗證現有報告的完整性和準確性 |
| `config` | 檢視或修改報告設定 |

## 選項

<ParamField path="--framework" type="string">
  目標合規框架。執行 `panguard report list-frameworks` 查看所有選項。
</ParamField>

<ParamField path="--language" type="string" default="en">
  報告內容語言（例如 `en`、`zh-TW`）。
</ParamField>

<ParamField path="--format" type="string" default="pdf">
  輸出格式：`pdf`、`html`、`json` 或 `csv`。
</ParamField>

<ParamField path="--output-dir" type="string">
  寫入產生報告的目錄。預設為目前工作目錄。
</ParamField>

<ParamField path="--org" type="string">
  要包含在報告標頭和中繼資料中的組織名稱。
</ParamField>

## 範例

<CodeGroup>
```bash 產生 ISO 27001 報告
panguard report generate --framework iso27001 --org "Acme Corp"
```

```bash 以中文產生 SOC 2 報告
panguard report generate --framework soc2 --language zh-TW
```

```bash 列出可用框架
panguard report list-frameworks
```

```bash 快速合規摘要
panguard report summary --framework nist-csf
```

```bash 匯出為 HTML
panguard report generate --framework tcsa --format html --output-dir ./reports
```

```bash 驗證現有報告
panguard report validate --output-dir ./reports
```
</CodeGroup>

## 支援的框架

| 框架 | 識別碼 | 說明 |
| --- | --- | --- |
| ISO 27001 | `iso27001` | 國際資訊安全標準 |
| SOC 2 | `soc2` | 服務組織信任準則 |
| NIST CSF | `nist-csf` | NIST 網路安全框架 |
| CIS Controls | `cis` | 網際網路安全中心關鍵控制措施 |
| Taiwan TCSA | `tcsa` | 台灣企業安全聯盟標準 |
| PCI DSS | `pci-dss` | 支付卡產業資料安全標準 |

## 方案要求

<Info>
  報告產生需要 **Pro**（$29/月）方案或更高。`list-frameworks` 和 `summary` 適用於所有方案。
</Info>

## 相關資源

<CardGroup cols={2}>
  <Card title="合規報告指南" icon="book-open" href="/guides/compliance-reports">
    產生您第一份合規報告的逐步指南。
  </Card>
  <Card title="Report 產品概覽" icon="file-lines" href="/products/report/overview">
    Report 系統的完整文件。
  </Card>
  <Card title="支援的框架" icon="scale-balanced" href="/products/report/frameworks">
    各框架的詳細控制對應。
  </Card>
  <Card title="panguard scan" icon="magnifying-glass" href="/cli/scan">
    報告從掃描資料產生 -- 請先執行掃描。
  </Card>
</CardGroup>
