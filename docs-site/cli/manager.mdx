---
title: "panguard manager"
description: "Start the centralized multi-endpoint management server."
icon: "server"
---

The `manager` command starts the Panguard Manager server -- a centralized dashboard and API for monitoring and controlling Guard agents across multiple endpoints. It provides fleet-wide visibility, policy management, and event aggregation.

## Usage

```bash
panguard manager [options]
```

## Options

<ParamField path="--port" type="number" default="9400">
  Port for the Manager web interface and API.
</ParamField>

<ParamField path="--auth-token" type="string">
  Bearer token for authenticating API requests. If not provided, a token is generated and printed at startup.
</ParamField>

<ParamField path="--max-agents" type="number" default="25">
  Maximum number of agent connections to accept. Default: 25.
</ParamField>

## Examples

<CodeGroup>
```bash Start Manager with defaults
panguard manager
```

```bash Custom port and token
panguard manager --port 8443 --auth-token my-secret-token
```

```bash Limit to 10 agents
panguard manager --max-agents 10
```
</CodeGroup>

## Features

| Feature | Description |
| --- | --- |
| **Fleet Dashboard** | Real-time overview of all connected agents |
| **Policy Engine** | Define and distribute security policies centrally |
| **Event Aggregation** | Unified event stream from all endpoints |
| **Agent Lifecycle** | Remote start, stop, upgrade, and configure agents |
| **API Access** | RESTful API for automation and integration |

<Tip>
  The Manager web interface is accessible at `http://localhost:9400` by default. For production deployments, place it behind a reverse proxy with TLS.
</Tip>

## Related

<CardGroup cols={2}>
  <Card title="Manager Product Overview" icon="sitemap" href="/products/manager/overview">
    Architecture and capabilities of the Manager system.
  </Card>
  <Card title="Agent Lifecycle" icon="arrows-spin" href="/products/manager/agent-lifecycle">
    How Manager controls remote agent instances.
  </Card>
  <Card title="Policy Engine" icon="scale-balanced" href="/products/manager/policy-engine">
    Centralized policy definition and enforcement.
  </Card>
  <Card title="panguard deploy" icon="rocket" href="/cli/deploy">
    Deploy agents to endpoints managed by Manager.
  </Card>
</CardGroup>
